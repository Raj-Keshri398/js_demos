"use strict" /*when we write "use strict" word in js the all code in js treat newer version*/ 

//alert(3 + 3) //alert keyword using in nodejs not browser 

let name = "shubham"
let age = 23
let islogged = false
let state; // variable declare but value not assign is called undefined.

// number => 2 to power 53
// bigint
// string => ""
// boolean => true/false
// null => it is the type of standlone value (ex: object) so we called null is an object.
// undefined => variable declare but value not assign
// symbol => when we talk about uniques then we use symbols


// object